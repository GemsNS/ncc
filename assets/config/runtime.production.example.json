{
  "apiBase": "/api",
  "prayerChat": {
    "useBackendAi": true,
    "persistThreads": true
  },
  "_comment": "Copy to runtime.json on the server at assets/config/runtime.json. Requires the web server (Apache or nginx) to proxy /api to the Node backend on the same host."
}
